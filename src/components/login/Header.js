import { PageHeader } from 'antd';

export const Header = (props) => {
  return (
    <PageHeader
      ghost={false}
      title="Hệ thống quản lý học viên"
      extra={[]}
    ></PageHeader>
  );
};
