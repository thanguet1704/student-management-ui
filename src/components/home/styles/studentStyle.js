import { makeStyles } from '@material-ui/core';

export const studentStyle = makeStyles({
  breadcrumbAttendence: {
    margin: '16px 0',
    display: 'flex',
    justifyContent: 'center',
    fontSize: '1.5rem',
  },
});
